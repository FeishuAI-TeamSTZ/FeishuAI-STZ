# 主架构 vs 传统RAG Baseline 对比测试用例
> 测试原则：所有用例在两个系统上**完全复用**，唯一变量为记忆引擎实现，其他变量（LLM模型、Embedding模型、上下文窗口、参数配置）完全一致

---

## 一、基础能力测试（对齐官方三大主测试）
### TC001：抗干扰记忆召回测试
| 项 | 说明 |
|----|------|
| **测试目标** | 验证大噪声下长期记忆的召回能力 |
| **测试步骤** | 1. 注入1条关键决策：`项目A上线日期定在2026-05-15`（来源：群聊@负责人确认）<br>2. 注入1000条无关噪声消息：<br>&nbsp;&nbsp;- 30% 日常闲聊/表情<br>&nbsp;&nbsp;- 30% 技术讨论（与项目A无关）<br>&nbsp;&nbsp;- 30% 其他独立决策<br>&nbsp;&nbsp;- 10% 乱码/图片/非文本内容<br>3. 模拟7天时间流逝（无任何相关操作）<br>4. 发起查询：`项目A什么时候上线？` |
| **输入** | 查询："项目A什么时候上线？" |
| **评估标准** | 返回结果包含正确日期"2026-05-15"即为通过 |
| **预期表现** | 主架构：≥90% 通过率<br>Baseline RAG：≤30% 通过率（被噪声淹没） |

---

### TC002：矛盾更新覆写准确率测试
| 项 | 说明 |
|----|------|
| **测试目标** | 验证决策迭代时返回最新版本的准确率 |
| **测试步骤** | 1. 注入初始决策：`周报每周五发给张三`<br>2. 间隔1小时注入新决策：`周报以后发给李四，不用发张三了`<br>3. 间隔1天注入更新：`周报调整为每周四发给李四，抄送给张三`<br>4. 发起查询：`周报现在要发给谁？` |
| **输入** | 查询："周报现在要发给谁？" |
| **评估标准** | 返回最新规则"每周四发给李四，抄送给张三"即为通过 |
| **扩展边界用例** | 同逻辑测试4类边界场景：<br>1. 跨源矛盾：群聊说A、文档写B<br>2. 时序错乱：新决策先收到、旧决策后收到<br>3. 软冲突："主要发给张三" vs "也抄送李四"<br>4. 多跳覆盖：A→B→C三次迭代 |
| **预期表现** | 主架构：≥95% 准确率<br>Baseline RAG：≤40% 准确率（可能召回旧版本） |

---

### TC003：跨源一致性检测测试
| 项 | 说明 |
|----|------|
| **测试目标** | 验证多源冲突信息的识别与处理能力 |
| **测试步骤** | 1. 飞书OKR系统同步：`Q3冻结重大版本发布`<br>2. 飞书文档同步：`项目A计划Q4上线`<br>3. 群聊新消息：`项目A下周发2.0版本`<br>4. 发起查询：`项目A下周能发版吗？` |
| **输入** | 查询："项目A下周能发版吗？" |
| **评估标准** | 主架构：识别跨源冲突，提示存在矛盾并给出优先级建议<br>Baseline：只要有一个来源说能发就返回能发 |
| **预期表现** | 主架构：100% 识别冲突<br>Baseline RAG：<10% 识别冲突 |

---

## 二、边界场景测试
### TC004：时序错乱处理测试
| 项 | 说明 |
|----|------|
| **测试目标** | 验证逻辑时间而非接收时间的时序对齐能力 |
| **测试步骤** | 1. 先收到延迟消息（发送时间T-3天）：`项目A上线时间改为5月20日`<br>2. 后收到新消息（发送时间T-1天）：`项目A上线时间确定为5月15日`<br>3. 查询：`项目A最终上线时间是哪天？` |
| **评估标准** | 返回5月15日即为通过 |
| **预期表现** | 主架构：100% 正确（按消息原始时间戳排序）<br>Baseline RAG：50% 概率返回错误（按接收时间排序） |

---

### TC005：模糊语义查询测试
| 项 | 说明 |
|----|------|
| **测试目标** | 验证非精确关键词查询的召回能力 |
| **测试步骤** | 1. 注入决策：`项目A上线前必须通过安全渗透测试`<br>2. 模糊查询：`项目A发版前有什么强制流程吗？` |
| **评估标准** | 返回提到安全渗透测试即为通过 |
| **预期表现** | 主架构：≥90% 召回率<br>Baseline RAG：≤60% 召回率 |

---

### TC006：低价值记忆遗忘测试
| 项 | 说明 |
|----|------|
| **测试目标** | 验证长期未访问低价值信息的自动降级能力 |
| **测试步骤** | 1. 注入两条决策：<br>&nbsp;&nbsp;决策A（高价值）：`Q3冻结发布`（多源确认，高频访问）<br>&nbsp;&nbsp;决策B（低价值）：`临时活动下周奶茶买一送一`（单源，无验证）<br>2. 模拟30天无访问<br>3. 分别查询两条决策 |
| **评估标准** | 决策A正常返回，决策B提示"信息可能已过期"即为通过 |
| **预期表现** | 主架构：100% 正确降级<br>Baseline RAG：100% 同等权重返回两条信息 |

---

## 三、效能体验测试
### TC007：回答准确率对比
| 项 | 说明 |
|----|------|
| **测试集** | 100条企业场景常见查询，包含40%冲突/迭代场景 |
| **评估标准** | 人工标注回答正确/错误 |
| **预期表现** | 主架构：≥85% 准确率<br>Baseline RAG：≤55% 准确率 |

---

### TC008：人工干预率对比
| 项 | 说明 |
|----|------|
| **测试周期** | 模拟7天使用，包含20次决策迭代/冲突场景 |
| **评估标准** | 统计需要人工纠正错误回答的次数 |
| **预期表现** | 主架构：≤5次干预<br>Baseline RAG：≥15次干预 |

---

### TC009：检索耗时对比
| 项 | 说明 |
|----|------|
| **测试场景** | 1000次随机查询，统计P95耗时 |
| **预期表现** | 主架构Hot路径：≤200ms<br>主架构Cold路径：≤3s<br>Baseline RAG：≤500ms |

---

## 四、测试数据集规范
### 基础数据集构成
| 类型 | 数量 | 说明 |
|------|------|------|
| 核心决策 | 100条 | 包含项目/规则/人员等各类型决策 |
| 迭代决策对 | 50组 | 同一条决策多次修改的序列 |
| 跨源冲突对 | 30组 | 不同来源的冲突信息 |
| 噪声消息 | 10000条 | 真实企业群聊脱敏数据 |

### 评分规则
- 主架构得分：(三大主测试得分 * 0.7) + (边界测试得分 * 0.2) + (效能得分 * 0.1)
- Baseline得分：相同计算规则
- 优势倍数 = 主架构得分 / Baseline得分，目标≥2倍
