# 飞书长程记忆引擎 - 量化指标说明文档

## 一、指标体系设计目标
建立可量化、可验证的评估标准，从**准确性、有效性、经济性**三个维度衡量记忆引擎的核心能力，确保系统上线后能达到预期业务价值。

---

## 二、详细指标说明

### 2.1 知识一致性维度 (Consistency)
#### 1. SSOT (唯一事实源) 达成率
- **定义**：面对多次修改的决策（如方案A→方案B→方案C），系统返回最新正确决策的次数占总查询次数的比例
- **计算公式**：`返回正确结果次数 / 总查询次数 × 100%`
- **测试方法**：
  1. 准备100条经历过≥2次版本变更的历史事实
  2. 对每条事实发起3次随机查询
  3. 统计返回最新版本结果的比例
- **目标值**：≥99%
- **优化方向**：提升事务性覆写成功率、优化RAG索引刷新时效性

#### 2. 冲突检出率
- **定义**：系统识别出新输入信息与旧记忆存在逻辑矛盾的成功率
- **计算公式**：`正确识别冲突次数 / 实际存在冲突的总次数 × 100%`
- **测试方法**：
  1. 构造测试集：50条存在语义冲突的输入 + 50条无冲突的正常输入
  2. 输入系统后统计正确识别/误判的次数
  3. 排除边缘歧义案例后计算准确率
- **目标值**：≥95%
- **优化方向**：优化实体对齐算法、完善同义词映射表、提升语义相似度计算准确率

---

### 2.2 检索质量维度 (Quality)
#### 1. 背景感知召回率 (Contextual Recall)
- **定义**：检索碎片化信息时，模型能正确关联其所属项目背景、时间线、关联方等上下文信息的比例
- **计算公式**：`正确返回完整上下文的次数 / 总查询次数 × 100%`
- **测试方法**：
  1. 准备50条跨项目、跨时间线的碎片化事实
  2. 查询时要求返回信息所属的项目、时间、相关负责人等上下文
  3. 统计上下文完全正确的比例
- **目标值**：≥90%
- **优化方向**：完善事实Schema的结构化属性、优化分层检索策略

#### 2. 抗干扰信噪比
- **定义**：在注入大量无关讨论噪音后，核心事实在检索结果中的排名稳定性
- **计算公式**：`注入噪音后排名 / 注入噪音前排名`
- **测试方法**：
  1. 先测试10条核心事实的正常检索排名
  2. 向记忆库中注入10倍体量的无关聊天内容噪音
  3. 再次检索相同事实，计算排名变化比例
- **目标值**：≥0.8（排名下降不超过20%）
- **优化方向**：提升事实置信度权重、优化记忆分层召回策略

---

### 2.3 效率与体验维度 (Efficiency)
#### 1. 主动预警准确率 (SNR)
- **定义**：用户主动确认更新的有效问询次数，与误报/骚扰问询次数的比例
- **计算公式**：`有效确认次数 / (误报次数 + 骚扰次数)`
- **测试方法**：
  1. 统计连续7天的所有主动问询记录
  2. 分类标记：有效确认/误报（实际无冲突）/骚扰（低价值问题）
  3. 计算比例
- **目标值**：≥4:1（每5次问询至少4次是有效）
- **优化方向**：优化阈值策略、完善打扰预算机制、提升冲突检测准确率

#### 2. 人工维护削减量
- **定义**：对比传统手动更新Wiki文档的模式，系统通过主动问询自动同步知识所节省的人工时间比例
- **计算公式**：`(手动更新耗时 - 系统辅助更新耗时) / 手动更新耗时 × 100%`
- **统计口径**：按月度统计团队所有知识更新的总耗时对比
- **目标值**：≥70%
- **优化方向**：提升自动更新准确率、减少需要人工确认的场景

#### 3. 用户满意度 (NPS)
- **定义**：用户对主动问询功能的净推荐值，衡量整体用户接受度
- **统计方法**：每月推送问卷，询问"你是否愿意向其他同事推荐该记忆功能？"（0-10分）
- **计算公式**：`(推荐者占比 - 贬损者占比) × 100`
- **目标值**：≥40分
- **优化方向**：降低误报率、优化卡片文案、合并低优先级问询

---

## 三、测试与迭代方法
1. **灰度测试阶段**：先在10人以内的小团队灰度运行2周，重点测试冲突检出率和主动预警准确率
2. **全量上线前**：必须通过所有核心指标的基准测试，未达标的模块优先优化
3. **上线后**：每周统计所有指标数据，每月做一次全维度评估，持续优化阈值和算法
4. **阈值调整原则**：优先保证SSOT达成率和主动预警准确率，不牺牲核心体验换覆盖率

---

## 四、指标权重优先级
| 优先级 | 指标 | 影响权重 |
|--------|------|----------|
| P0 | SSOT达成率 | 40% |
| P0 | 主动预警准确率 | 30% |
| P1 | 冲突检出率 | 10% |
| P1 | 背景感知召回率 | 10% |
| P2 | 其他指标 | 10% |
