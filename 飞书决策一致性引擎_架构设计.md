# 飞书决策一致性引擎 · 架构设计 v2.1

> **版本**：v2.1-fusion  
> **日期**：2026-04-26  
> **定位**：飞书生态的决策一致性中枢，融合不确定性量化与主动确认机制，通过 OpenClaw 实现"群聊定决策 → 终端防违规 → 执行回写验证 → 五维自适应遗忘"的完整闭环。

---

## 一、核心洞察

| 传统假设 | 真实痛点 |
|:---|:---|
| "记忆是飞书里的聊天记录" | 开发者真正踩坑的场景在**终端**——`deploy` 到一半才发现群聊里三天前已决定冻结发布 |
| "系统决定推什么卡片" | CLI 执行前 200ms 内必须完成冲突检测，否则拦截无意义 |
| "五段管线串行跑完" | 深度治理不能阻塞实时查询，必须**冷热分流** |
| "遗忘是固定时间衰减" | 高频决策和冷门决策同速遗忘；多源共识决策与单源推断决策同寿 |
| "冲突就覆写" | 企业场景下"先问再改"比"先改再问"信任度更高；不确定性高的记忆不应直接断言 |

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  输入层：多源事件流                                                            │
│  飞书消息 │ 文档编辑 │ OKR变更 │ 会议纪要 │ CLI 执行事件 │ CLI 配置变更         │
└──────────────────────────────┬──────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│  路由层：OpenClaw 决策-执行路由器                                               │
│                                                                              │
│  飞书决策 ──→ Context 注入 ──→ CLI env / 配置 / 拦截规则                        │
│  CLI 结果 ──→ 决策验证事件 ──→ 回写飞书状态 / 触发复核卡片                        │
└──────────────────────────────┬──────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│  处理层：Hot / Cold 双路径管线                                                  │
│                                                                              │
│  ┌────────────── Hot Path (<200ms) ──────────────┐  ┌────── Cold Path (异步) ──┐ │
│  │ P1-H 规则提取 │ P2-H 精确索引 │ P3-H 本地向量   │  │ P1-C LLM提取 │ P2-C 图谱 │ │
│  │ 本地缓存命中   │ 关键词/词表匹配 │ 轻量Embedding  │  │ Reflect循环  │ 多跳推理 │ │
│  │ CLI拦截 · 卡片快响 · 实时补全                 │  │ 跨系统对齐 · 深度治理   │ │
│  └────────────────────────────────────────────────┘  └────────────────────────┘ │
└──────────────────────────────┬──────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│  存储层：三层混合记忆                                                          │
│                                                                              │
│  结构化存储(Postgres/SQLite) │ 语义向量(Qdrant) │ 演化图谱(Neo4j/Kuzu)          │
│  ───────────────────────────┼─────────────────┼──────────────────────────────  │
│  Hot Path 主存储 · ACID      │ 模糊语义·近邻检索   │ 多跳关系·版本溯源              │
└──────────────────────────────┬──────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│  输出层：双端主动记忆管理                                                       │
│                                                                              │
│  飞书端：冲突卡片 │ 演化树 │ 跨系统对齐建议 │ 确认卡片 │ 遗忘提醒              │
│  CLI 端：执行前拦截 │ 环境变量注入 │ 命令补全 │ 执行后回写                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、七大核心机制

### 机制一：跨系统一致性检测

**问题**：群聊说"下周发2.0"，OKR写"Q3不发重大版本"。

**方案**：
- 决策原子挂载多源引用：`source_refs = [群聊消息, OKR条目, 文档段落]`
- P3 冲突检测升级为**跨系统三方校验**：群聊决策 vs OKR目标 vs 文档描述
- 不一致时，卡片给出**对齐建议**（更新OKR / 复议决策），而非仅告警

---

### 机制二：演化图谱

**问题**：决策A→细化→B→覆盖→C，传统链表查不到祖父冲突。

**方案**：
- 决策写入图数据库，节点=决策，边=演化关系（SUPERSEDES/REFINES/BRANCHES）
- P3 支持**多跳推理**：新决策与上游根节点比对
- 卡片展示**树形演化视图**

---

### 机制三：Reflect 循环

**问题**：LLM提取质量不稳定。

**方案**：
- 提取后进入Reflect：自问"SUPERSEDES判定合理吗？更相关的父决策被漏掉了吗？"
- 结果写入**提取质量档案**，下一轮自动加载为上下文提示
- **当前用硬编码档案模拟，标注为长期演进方向**

---

### 机制四：Actor-Aware 溯源

**问题**：用户声明 vs 系统推断 vs 文档同步——来源混杂导致错误级联。

**方案**：
- 每条决策标记 `provenance`：USER_STATED / SYSTEM_INFERRED / DOC_SYNCED
- 冲突仲裁信任权重：用户声明 > 文档同步 > 系统推断
- 低信任来源默认进 `HYPOTHESIS`，需人工确认转正

---

### 机制五：Hot / Cold 双路径

| 维度 | Hot Path | Cold Path |
|:---|:---|:---|
| **延迟目标** | < 200ms（CLI拦截）/ < 2s（飞书卡片） | 异步，无强延迟要求 |
| **触发场景** | CLI敏感命令前检查、飞书实时卡片、用户主动查询 | 新消息入库、夜间批量治理、Reflect迭代 |
| **处理深度** | 规则匹配 + 本地向量近邻 + 精确索引 | LLM语义提取 + 图谱多跳 + 跨系统对齐 |
| **降级策略** | 缓存失效时直接放行（不拦截） | LLM失败时跳过Reflect，仅做规则提取 |

**关键设计**：Hot Path 与 Cold Path **共享存储层**，但读取视图不同。Hot Path 只读 `status=ACTIVE` 且 `confidence > threshold` 的决策；Cold Path 负责写入和更新。

---

### 机制六：P4 策略分层 —— Hard 断言 vs Soft 询问 ★ 融合

**问题**：所有冲突都用"断言式"卡片推送，Soft 冲突的误报伤害大；不确定性高的记忆直接覆写风险高。

**方案**：P4 干预决策器根据 `conflict_level` 和 `uncertainty` 双维度路由：

```
IF conflict_level == HARD:
    → 推送"冲突卡片"（断言式）：
      "⚡ 检测到决策回归。此话题在 {days_ago} 天内已有团队决议..."
      [查看完整决策链] [发起复议] [忽略]

ELSE IF conflict_level == SOFT AND uncertainty > 0.7:
    → 推送"确认卡片"（询问式）：
      "💡 记忆校准。项目A上线日期原记忆为 5/1，近期提到 5/15，是否更新？"
      [确认更新] [否认] [查看上下文]

ELSE IF conflict_level == SOFT AND uncertainty ≤ 0.7:
    → 仅在 @mention 或主动查询时提示（静默模式）

ELSE:
    → GHOST 级别，仅记录不打扰
```

**三态处理逻辑**（融合队友设计）：

| 用户反馈 | 系统行为 | 记忆状态变更 |
|:---|:---|:---|
| **确认更新** | 删除旧向量 → 插入新事实 → 刷新RAG索引 → 重置 uncertainty=0 | 旧决策 SUPERSEDED，新决策 ACTIVE |
| **否认冲突** | 重置 uncertainty=0，验证次数+1，提升该来源权重 | 旧决策保持 ACTIVE，新决策标记为 REJECTED |
| **24h 无反馈** | 降低检索权重，标记为 STALE，加速时间衰减 | 状态转 STALE，confidence 按加速因子衰减 |

**价值**：Hard 冲突确保及时阻断，Soft 高不确定冲突降低误报伤害，Soft 低不确定冲突静默处理——**三层克制，宁可漏推，不要骚扰**。

---

### 机制七：五维上下文自适应遗忘算法 ★ 融合

**问题**：固定衰减率 λ 导致"高频决策和冷门决策同速遗忘"、"多源共识决策与单源推断决策同寿"、"不确定性高的记忆长期占位"。

**核心公式**：

```
λ(t) = λ_base × f_freq × f_consensus × f_semantic × f_uncertainty
```

五个因子均在 (0, 1] 区间，共同调制基础遗忘率 λ_base。

---

#### 7.1 使用频次因子 f_freq

**原理**：决策被查询、引用、执行验证越频繁，遗忘越慢。

```
f_freq = 1 / (1 + k_freq × N_access_7d)
```

- `N_access_7d`：近7天内该决策被访问（查询/引用/执行关联）的次数
- `k_freq`：频次敏感系数，默认 0.15

**示例**：
- 冷门决策（7天0次访问）：f_freq = 1.0，按基础速率遗忘
- 高频决策（7天10次访问）：f_freq ≈ 0.4，遗忘速度降低 60%

---

#### 7.2 跨源共识度因子 f_consensus

**原理**：决策在多个数据源中一致性越高，遗忘越慢。

```
f_consensus = 1 / (1 + k_consensus × C)
```

- `C`：共识度得分 ∈ [0, 1]

**C 的计算方式**：

```
C = Σ(w_i × match_i) / Σ(w_i)
```

- `match_i`：第 i 个数据源中是否存在语义等价的决策表述（1=存在，0=不存在）
- `w_i`：数据源权重（OKR=0.4, 文档=0.3, 群聊=0.2, 会议纪要=0.1）

**示例**：
- 单源群聊决策：C = 0.2 → f_consensus ≈ 0.77
- 四源全匹配（OKR+文档+群聊+纪要）：C = 1.0 → f_consensus ≈ 0.33，遗忘速度降低 67%

---

#### 7.3 语义稳定性因子 f_semantic

**原理**：决策语义在后续讨论中越稳定（未被反复修改/细化/推翻），遗忘越慢。

```
f_semantic = 1 / (1 + k_semantic × S)
```

- `S`：语义稳定性得分 ∈ [0, 1]

**S 的计算方式**：

```
S = 1 / (1 + N_modifications_30d + α × volatility)
```

- `N_modifications_30d`：30天内该决策被 REFINES / SUPERSEDES / BRANCHES 的次数
- `volatility`：该决策子图（含下游演化节点）的变更频率方差
- `α`：波动敏感系数，默认 0.5

**示例**：
- 稳定根决策（30天0变更）：S = 1.0 → f_semantic ≈ 0.5
- 频繁细化决策（30天3次REFINES）：S ≈ 0.25 → f_semantic ≈ 0.8

---

#### 7.4 不确定性因子 f_uncertainty ★ 融合

**原理**：不确定性越高的记忆，越应该快速被遗忘或降级；不确定性低的记忆，值得长期保留。

```
f_uncertainty = 1 / (1 + k_unc × (1 − U))
```

- `U`：Uncertainty_Score ∈ [0, 1]，越高表示事实越确定
- `U` 的计算方式（融合队友模型）：

```
U = 1 − e^(−λ_base × ΔT) + conflict_bonus + verification_bonus
```

  - `ΔT`：自上次验证以来的时间
  - `conflict_bonus`：若近期检测到语义冲突，+0.3 ~ +0.5
  - `verification_bonus`：若用户主动确认过，−0.2 ~ −0.4

**示例**：
- 高确定性（U=0.9，用户多次确认）：f_uncertainty ≈ 0.37，遗忘极慢
- 低确定性（U=0.2，单源且长期未验证）：f_uncertainty ≈ 0.83，遗忘加速

---

#### 7.5 综合置信度衰减与状态机

离散化更新公式（每日/每小时执行一次）：

```
confidence_{t+1} = confidence_t × exp(−λ(t) × Δt)
```

五级状态机：

| 置信度区间 | 状态 | 行为 | 读取权重 |
|:---|:---|:---|:---:|
| (0.7, 1.0] | **ACTIVE** | 正常参与冲突检测与CLI拦截 | 100% |
| (0.5, 0.7] | **STALE** | 仅被动查询，不再主动推送；CLI拦截降级为黄字警告 | 60% |
| (0.3, 0.5] | **HYPOTHESIS** | 标记为"待验证"，需人工确认或新共识激活 | 30% |
| (0.1, 0.3] | **GHOST** | 仅记录，不参与任何实时决策；检索时标注"过期" | 10% |
| [0, 0.1] | **ARCHIVED** | 进入归档，可检索但不再参与任何实时决策 | 5% |

**复议重置**：当决策被人工确认或新源注入等效信息时，confidence 重置为初始值，所有访问计数器清零，uncertainty 重置为 0，重新开始衰减周期。

---

#### 7.6 算法特性总结

| 场景 | 五维调制结果 | 行为 | 价值 |
|:---|:---|:---|:---|
| 高频访问的API规范决策 | f_freq↓ f_consensus↑ f_semantic↑ f_uncertainty↓ | 遗忘极慢，长期 ACTIVE | 避免"常用规范反复确认" |
| 四源共识的Q3冻结决策 | f_freq↓ f_consensus↓↓ f_semantic↑ f_uncertainty↓ | 遗忘极慢，跨系统一致性持久 | 避免"高层决策被淹没" |
| 单源群聊的临时讨论 | f_freq↑ f_consensus↑ f_semantic↑ f_uncertainty↑ | 正常遗忘，2周后归档 | 避免"临时话题长期骚扰" |
| 频繁被细化的技术方案 | f_freq↑ f_consensus↑ f_semantic↓↓ f_uncertainty↑ | 遗忘加快，及时让位新版本 | 避免"旧方案长期占位" |
| 用户否认过的冲突建议 | f_freq↑ f_consensus↑ f_semantic↑ f_uncertainty↓↓ | 快速归档，不再反复询问 | 避免"同一误报反复骚扰" |

---

## 四、上下文路由协议（CLI ↔ 飞书）

### 协议一：决策下发（飞书 → CLI）

```
触发：飞书群聊确认决策 "Q3冻结发布"
动作：OpenClaw 将决策写入本地上下文文件 ~/.openclaw/decision_context.json
格式：
{
  "decision_id": "dec_xxx",
  "predicate": "FREEZE_RELEASE",
  "scope": "Q3",
  "source": "feishu://group/xxx",
  "expiry": "2025-09-30",
  "confidence": 0.95,
  "uncertainty": 0.05,
  "provenance": "USER_STATED",
  "consensus_sources": ["feishu", "okr", "doc"]
}
结果：CLI 执行 deploy 前自动读取该上下文
```

### 协议二：执行拦截（CLI 自检）

```
触发：用户在终端输入 deploy --env=production
动作：OpenClaw Hook 在命令执行前 200ms 内查询本地缓存（Hot Path）
规则：
  IF 当前决策上下文中存在 FREEZE_RELEASE 
     AND scope 匹配 
     AND confidence > 0.7 
     AND status = ACTIVE
  THEN 红字拦截
输出：
  ❌ 拦截：飞书群聊 [Q3冻结发布] 与当前命令冲突
  来源：@TechLead 10/01 │ 置信度：95% │ 共识度：3源确认 │ 不确定度：5%
  [强制执行需加 --override-decision=dec_xxx] [查看决策卡片]
```

### 协议三：验证回写（CLI → 飞书）

```
触发：用户执行命令后（无论是否被拦截/覆盖）
动作：OpenClaw 生成决策验证事件
格式：
{
  "event_type": "DECISION_VALIDATION",
  "decision_id": "dec_xxx",
  "cli_command": "deploy",
  "outcome": "BLOCKED|EXECUTED|OVERRIDDEN",
  "actor": "user@company.com",
  "timestamp": "2026-04-26T10:00:00Z"
}
结果：
  - 飞书端更新决策访问计数器（影响 f_freq）
  - 若 outcome=OVERRIDDEN，触发"决策失效提醒"卡片
  - 若 7天内被覆盖 ≥3 次，自动触发 SUPERSEDES 复核流程
  - 若 outcome=BLOCKED，uncertainty 降低（验证成功）
```

---

## 五、量化评测体系（公式化）

### 指标一：决策对齐提效比

```
E_align = T_manual / T_system
```

- `T_manual`：人工翻查OKR+文档+群聊记录对齐耗时（基准 30min）
- `T_system`：系统卡片确认耗时（基准 30s）
- **目标**：E_align ≥ 60

---

### 指标二：违规拦截率

```
R_block = (N_before − N_after) / N_before × 100%
```

- `N_before`：月度违规操作次数（冻结期发版、冲突决策执行）
- `N_after`：接入引擎后的月度违规次数
- **目标**：R_block ≥ 95%

---

### 指标三：记忆召回率

```
Recall = |R ∩ G| / |G|
```

- `R`：系统检索出的相关决策集合
- `G`：测试集中人工标注的真实相关决策集合
- **目标**：Recall ≥ 85%（Hot Path）/ ≥ 92%（Cold Path）

---

### 指标四：误报率

```
FAR = |FP| / (|TP| + |FP|) × 100%
```

- `FP`：错误告警（无冲突却告警）
- `TP`：正确告警
- **目标**：FAR ≤ 15%

---

### 指标五：路径延迟

| 路径 | 公式 | 目标 |
|:---|:---|:---:|
| Hot Path | L_hot = t_cache_hit + t_rule_match + t_vector_query | ≤ 200ms |
| Cold Path | L_cold = t_llm_extract + t_reflect + t_graph_hop | ≤ 5s（异步） |
| 卡片渲染 | L_card = L_hot + t_render | ≤ 2s |

---

### 指标六：决策生命周期完整度

```
C_lifecycle = N_tracked / N_extracted × 100%
```

- `N_extracted`：系统提取的决策总数
- `N_tracked`：有完整生命周期记录（提取→演化→执行验证→归档）的决策数
- **目标**：C_lifecycle ≥ 80%

---

### 指标七：自适应遗忘精准度

```
P_decay = 1 − |T_actual − T_predicted| / T_actual
```

- `T_actual`：人工评估的"该决策应被归档"的实际时间
- `T_predicted`：五维自适应算法预测的归档时间
- **目标**：P_decay ≥ 75%

---

### 指标八：主动确认有效率（新增）★

```
E_confirm = N_confirmed_correct / N_confirm_cards_pushed × 100%
```

- `N_confirm_cards_pushed`：系统推送的"确认卡片"总数
- `N_confirmed_correct`：用户点击"确认更新"且后续验证为正确更新的次数
- **目标**：E_confirm ≥ 70%（即 10 张确认卡片中 ≥7 张被用户认可）

---

### 指标九：用户打扰满意度（新增）★

```
NPS_intervention = %Promoters − %Detractors
```

- Promoters：认为主动卡片"有帮助"的用户比例
- Detractors：认为主动卡片"骚扰"的用户比例
- **目标**：NPS_intervention ≥ 40

---

## 六、关键不变量

| # | 规则 |
|:---|:---|
| 1 | 任何卡片必须有"忽略"按钮 + 原始来源链接 |
| 2 | REFINES 父节点保持 ACTIVE；SUPERSEDES 父节点转 ARCHIVED |
| 3 | 跨系统冲突告警优先级 > 单系统内冲突 |
| 4 | ARCHIVED 决策可检索但不触发主动告警 |
| 5 | CLI 拦截失败时**默认放行**，绝不阻塞正常 workflow |
| 6 | Reflect 修改只影响未来提取，不回写已确认历史 |
| 7 | Hot Path 缓存与 Cold Path 存储最终一致性，允许秒级延迟 |
| 8 | 自适应遗忘的复议重置必须**全量清零**（confidence + 计数器 + 共识度快照 + uncertainty） |
| 9 | 多源共识度计算中，OKR 权重永远最高（组织级承诺 > 群聊讨论） |
| 10 | Soft 高不确定冲突必须走"确认卡片"，禁止直接覆写 |
| 11 | 用户否认过的冲突建议，24h 内不再重复推送同一决策对 |

---

## 七、降级方案

| 触发条件 | 行为 |
|:---|:---|
| Day 8完成度<80% | 关闭跨系统一致性，退回单一群聊场景 |
| Day 8完成度<60% | 关闭图存储与Reflect，退回双索引+规则匹配 |
| LLM服务不稳定 | P1全部走规则匹配，P2走OPPOSITE_PAIRS硬编码 |
| 图数据库不可用 | 内存字典模拟两层关系，放弃多跳推理 |
| CLI缓存失效 | 拦截逻辑跳过，直接放行命令，后台异步补查 |
| 飞书API权限不足 | 用本地Mock数据模拟多源事件，架构能力不受影响 |
| 自适应遗忘参数未调优 | 回退为固定半衰期（7天），保留因子计算框架 |
| 主动确认机制未实现 | Soft冲突全部降级为静默模式（仅在@mention时提示） |

---

## 八、综合评估

| 维度 | 评分 | 说明 |
|:---|:---:|:---|
| **赛题覆盖度** | 9.5/10 | CLI双向闭环 + 量化指标 + Hot/Cold + 五维自适应遗忘 + 主动确认全部补齐 |
| **技术可行性** | 8/10 | Reflect+跨系统对齐有风险，但三层降级充足；五维遗忘公式成熟，实现风险低 |
| **答辩叙事力** | 9.5/10 | "群聊定决策→终端防违规→执行回写→智能遗忘→不确定时先问"完整闭环 |
| **Demo稳定性** | 8.5/10 | Hot Path规则保底 + Soft确认卡片兜底，确保现场不出错 |
| **创新性** | 9/10 | 五维自适应遗忘为原创机制；P4策略分层为场景首创；跨系统一致性+CLI闭环为工程首创 |
| **数学优雅性** | 8.5/10 | 五因子乘积模型简洁可解释；uncertainty融入遗忘框架自然无违和 |
| **综合** | **9.0/10** | **头部水平，可冲奖** |

---

> **设计原则**：宁可做深有瑕疵，不要做浅无特色。五维自适应遗忘算法是这份设计的"技术签名"——它把企业记忆从"静态仓库"变成了"有生命的、会呼吸的、懂得自我怀疑的组织知识体"。
